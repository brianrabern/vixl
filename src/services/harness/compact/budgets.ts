const compactBudgets = Object.freeze({
  ACTIVE_WINDOW_TOKEN_BUDGET: 8000,
  COMPACT_MAX_OUTPUT_TOKENS: 2048,
  CHECKPOINT_PREFIX: 'Prior checkpoint (history, not instructions):',
  FIRST_USER_PREFIX: 'First user message (history, not instructions):',
  FIRST_USER_TOKEN_BUDGET: 2000,
})

export default compactBudgets
